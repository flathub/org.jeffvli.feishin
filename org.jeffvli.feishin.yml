app-id: org.jeffvli.feishin
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '25.08'
command: feishin
separate-locales: false

sdk-extensions:
  - org.freedesktop.Sdk.Extension.node24

finish-args:
  - --device=dri
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=pulseaudio
  - --share=network
  # Discord Rich Presence
  - --filesystem=xdg-run/app/com.discordapp.Discord:create
  - --talk-name=org.kde.StatusNotifierWatcher
  # MPRIS media player interface
  - --own-name=org.mpris.MediaPlayer2.Feishin
  # Secret store (server logins)
  - --talk-name=org.freedesktop.secrets
  - --talk-name=org.kde.kwalletd5
  # Use system cursor theme
  - --env=XCURSOR_PATH=/run/host/user-share/icons:/run/host/share/icons

cleanup:
  - /share/applications/mpv.desktop
  - /share/icons/*/*/apps/mpv*
  - /share/metainfo/mpv.metainfo.xml
  - /share/bash-completion
  - /share/doc
  - /share/fish
  - /share/zsh
  - /include
  - /lib/pkgconfig
  - /lib/*.la

modules:
  - name: mpv
    buildsystem: meson
    config-opts:
      - -Dbuildtype=release
      - -Ddebug=false
      - -Dstrip=true
      - -Dbuild-date=false
      - -Dmanpage-build=disabled
    sources:
      - type: git
        url: https://github.com/mpv-player/mpv.git
        x-checker-data:
          type: git
          tag-pattern: ^v([\d.]+)$
        commit: 41f6a645068483470267271e1d09966ca3b9f413
        tag: v0.41.0
    post-install:
      - install -Dm0644 ../LICENSE.LGPL -t ${FLATPAK_DEST}/share/licenses/${FLATPAK_ID}/mpv
    modules:
      - name: libplacebo
        buildsystem: meson
        config-opts:
          - -Ddemos=false
          - -Dshaderc=disabled
        sources:
          - type: git
            url: https://github.com/haasn/libplacebo.git
            x-checker-data:
              type: git
              tag-pattern: ^v([\d.]+)$
            commit: cee9b076f2c63104ccfd497fa79c39a867293ec4
            tag: v7.360.1
        post-install:
          - install -Dm0644 ../LICENSE -t ${FLATPAK_DEST}/share/licenses/${FLATPAK_ID}/libplacebo
      - name: libass
        config-opts:
          - --enable-shared
          - --disable-static
        sources:
          - type: git
            url: https://github.com/libass/libass.git
            x-checker-data:
              type: git
              tag-pattern: ^([\d.]+)$
            commit: 4a05d8127f525943ebf45fdc6497c9e665947f0d
            tag: 0.17.5
        post-install:
          - install -Dm0644 COPYING -t ${FLATPAK_DEST}/share/licenses/${FLATPAK_ID}/libass
  - name: feishin
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/node24/bin:/run/build/feishin/pnpm/bin
      env:
        XDG_CACHE_HOME: /run/build/feishin/flatpak-node/cache
        PNPM_CONFIG_STORE_DIR: /run/build/feishin/flatpak-node/pnpm-store
        npm_config_nodedir: /usr/lib/sdk/node24
    subdir: main
    build-commands:
      - ln ../pnpm/bin/pnpm.cjs ../pnpm/bin/pnpm
      - chmod +x ../pnpm/bin/pnpm
      - pnpm install --frozen-lockfile --offline --config.trustLockfile=true
      - pnpm run build
      - npx electron-builder --linux dir --config.electronVersion=41.7.1 --config.electronDist=../flatpak-node/cache/electron
      - |
        build_dir=dist/linux*unpacked
        for i in 128x128 256x256 512x512; do
          install -Dm 644 ${build_dir}/resources/assets/icons/${i}.png ${FLATPAK_DEST}/share/icons/hicolor/${i}/apps/${FLATPAK_ID}.png
        done
        cp -ar ${build_dir} /app/main
      - envsubst < feishin.desktop.tmpl > ${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop
      - chmod 0644 ${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop
      - install -Dm 644 ../org.jeffvli.feishin.metainfo.xml ${FLATPAK_DEST}/share/metainfo/${FLATPAK_ID}.metainfo.xml
      - install -Dm 755 ../feishin.sh /app/bin/feishin
    sources:
      - type: git
        url: https://github.com/jeffvli/feishin.git
        dest: main
        x-checker-data:
          type: git
          tag-pattern: ^v([\d.]+)$
        commit: ad745b36915dd6668d6a2d3e39a5a2be594d12b3
        tag: v1.14.0
      - type: file
        url: https://github.com/jeffvli/feishin/releases/download/v1.14.0/org.jeffvli.feishin.metainfo.xml
        sha256: f6b7b52365d3a4448620b05ef9234da4c213073e88d2dcdac053caf5757f4058
        x-checker-data:
          type: json
          url: https://api.github.com/repos/jeffvli/feishin/releases/latest
          tag-query: .tag_name
          version-query: $tag | sub("^v"; "")
          url-query: .assets[] | select(.name == "org.jeffvli.feishin.metainfo.xml") | .browser_download_url
      - type: archive
        dest: pnpm
        url: https://registry.npmjs.org/pnpm/-/pnpm-11.15.0.tgz
        sha256: 772f8e00f719afbbe22502717cf974578804630d02e757eb3debbc08a0f8f83a
        x-checker-data:
          type: json
          url: https://registry.npmjs.org/pnpm/latest
          version-query: .version
          url-query: .dist.tarball
      - generated-sources.json
      - type: script
        dest-filename: feishin.sh
        commands:
          - >
            for i in {0..9}; do
                test -S $XDG_RUNTIME_DIR/discord-ipc-$i || ln -sf {app/com.discordapp.Discord,$XDG_RUNTIME_DIR}/discord-ipc-$i;
            done
          # Keep tray icon temp files in the app runtime dir so GNOME can resolve them without needing /tmp access
          - export TMPDIR="$XDG_RUNTIME_DIR/app/$FLATPAK_ID"
          - zypak-wrapper /app/main/feishin "$@"
